# Supply-chain hygiene (§7.6): never run lifecycle scripts from dependencies in CI.
# Locally, `pnpm install` will prompt before running scripts for new packages.
strict-peer-dependencies=false
auto-install-peers=true
engine-strict=true
