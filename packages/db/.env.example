# Local only. Never commit a real .env (root .gitignore excludes it).
# The app/tests should connect as a NON-superuser member of harbour_app so RLS actually applies;
# see prisma/migrations/0002_rls_and_guards/migration.sql and README.md.
DATABASE_URL="postgresql://harbour:harbour@localhost:5432/harbour?schema=public"
